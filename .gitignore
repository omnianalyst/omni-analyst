__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.env
.DS_Store
.opencode-session.db*
vendor/
_orchestrator/logs/
_orchestrator/worktrees/
_orchestrator/queue-state/

# Operator-local context: the session handoff and the orchestrator archive
# (work orders, agent reports, superseded docs) are private working files.
docs/NEXT_SESSION.md
_orchestrator/
docs/research/calls_ledger.jsonl
ops/call_tracker.config.json

# Defensive: transient operator/agent context must never land in a public
# history. NEXT_SESSION.md above is the deliberate private file; these catch
# the filenames a future session might invent for the same purpose.
SESSION_NOTES.md
*_NOTES.md
HANDOFF*.md
*HANDOFF*.md
.claude/
.opencode/
